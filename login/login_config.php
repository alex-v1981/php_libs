<?php

/*
class LoginConfig
{
    const MAIN_PAGE = 'index.php';
    const TITLE = "Site title";

    const LOGIN_KEY_PREFIX = 'app_xxx_';
    const LOGIN_USE_USERNAME = true;
    const LOGIN_USE_REMEMBER = true;

    const LOGIN_LIB_PATH = '/service_xxx/libs/login/';  // // with slash at the end

    public static function accounts()
    {
        return array(
            array('login'=>'username1', 'password'=>'xxx', 'is_admin'=>true),
            array('login'=>'username2', 'password'=>'xxx', 'is_admin'=>false)
        );
    }
}
 */

require_once 'config.php';

